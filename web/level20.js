function priSpusteni() {


    // Sem napis jen 3 prikazy
    // (misto komentaru nize):

    while (notFinished()) {
        if (isPathForward()) {
            // Proved prikazy zde
        } else {
            if (isPathRight()) {
                // Proved prikazy zde
            } else {
                // Jinak proved prikazy zde
            }
        }
    }


}
