function priSpusteni() {


    // Sem vepis prikazy pro Reda
    // Zde jsou ukazkove prikazy:
    turnLeft();
    turnRight();
    moveForward();

    // Opakuj 3krat
    for (var count1 = 0; count1 < 3; count1++) {
        // Prikazy, co se maji opakovat
    }

    // Opakuj 5krat
    for (var count2 = 0; count2 < 5; count2++) {
        // Prikazy, co se maji opakovat
    }

    // Cykly lze do sebe i zanorovat
    for (var count3 = 0; count3 < 6; count3++) {
        // Prikazy, co se maji opakovat
        for (var count4 = 0; count4 < 8; count4++) {
            // Prikazy, co se maji opakovat
        }
    }


}
